import { BrowserRouter, Routes, Route } from "react-router-dom"

const Router = () => {
    return <BrowserRouter>
        <Routes>
            {/* 여기에 라우터 등록 */}
        </Routes>
    </BrowserRouter>
}

export default Router